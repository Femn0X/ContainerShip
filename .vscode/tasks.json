{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run go",
            "command":"go run",
            "args": [
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}