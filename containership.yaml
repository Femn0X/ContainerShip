version: "1.0"
services:
 web:
  image: nginx:latest
  depends_on: [db]
  ports:
   - "8080:80"
  wait_for:
   - db:healthy
 db:
  image: postgres:15
  environment:
   POSTGRES_PASSWORD: defNotApi
 worker:
  image: busybox:latest
  depends_on: [db]